import appTheme, { COLORS, SIZES, FONTS } from "./Theme";
import images from "./images";
import icons from "./icons";
import fontsDefinition from "./fonts";

export { appTheme, icons, images, COLORS, FONTS, SIZES, fontsDefinition };
